# 局域网部署配置 —— 复制为 .env 使用（setup 脚本会自动生成并填好，一般无需手改）

# 登录凭证签名密钥（必填，强随机）。
# ⚠ 不要泄露、不要随意更换——换了之后所有人都要重新登录。
JWT_SECRET=

# 网页端口。默认 80，浏览器直接输 IP 即可（http://192.168.1.50/）。
# 若 80 被占用（Windows 上 IIS、迅雷等会占），改成 8080，
# 访问地址就变成 http://192.168.1.50:8080/
WEB_PORT=80
