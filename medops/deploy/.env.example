# 部署环境变量 —— 复制为 .env 使用（scripts/first-run.sh 会自动生成并填好）
# JWT 签名密钥（必填；first-run.sh 自动生成强随机值，勿泄露、勿随意更换——换了所有人都要重新登录）
JWT_SECRET=
# 站点域名，不带 https:// 前缀（如 jx.example.com）
DOMAIN=
# 证书到期提醒邮箱（可留空；留空则不注册邮箱）
CERT_EMAIL=
